# Dependencies
node_modules
.pnp
.pnp.js

# Build
.next
out
build
dist

# Env — regola generale: ogni variante .env* resta fuori da git,
# tranne i template .example (senza segreti reali).
.env
.env.*
!.env.example
!.env.staging.example
.env.local
.env.*.local
.env.staging.local
.env.production

# Chiavi e credenziali: mai in git
*.pem
*.p12
*.pfx
service-account*.json
*credentials*.json

# Local uploads (filesystem fallback)
.uploads

# Debug / IDE
*.log
npm-debug.log*
tsconfig.tsbuildinfo
.DS_Store
.idea
.vscode

# Vercel
.vercel

# Playwright
/e2e/.auth/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.cache/

# Worker scraping: credenziali prod separate
.env.worker

# Materiale sorgente/documentale (xlsx, PDF piani TIM, CSV, prototipo dismesso): resta su disco, fuori da git
Modulo - Inserimenti negozio/

# Backup env locali
.env.localhost-backup
